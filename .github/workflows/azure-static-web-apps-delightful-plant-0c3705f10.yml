name: Azure Static Web Apps CI/CD

on:
  push:
    branches:
      - pipeline-testing
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches:
      - pipeline-testing

jobs:
  # setup_env:
  #   runs-on: ubuntu-latest
  #   name: Set Environment Variables for Build

  #   steps:
  #     - name: Set REACT_APP_OKTA_CLIENT_ID from GitHub Secrets
  #       run: echo "REACT_APP_OKTA_CLIENT_ID=${{ secrets.REACT_APP_OKTA_CLIENT_ID }}" >> $GITHUB_ENV
  #     - name: Set REACT_APP_OKTA_ISSUER from GitHub Secrets
  #       run: echo "REACT_APP_OKTA_ISSUER=${{ secrets.REACT_APP_OKTA_ISSUER }}" >> $GITHUB_ENV
  setup_env:
    runs-on: ubuntu-latest
    name: Set Environment Variables for Build

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Create .env File
        run: |
          touch .env
          echo "REACT_APP_OKTA_CLIENT_ID=dummy" >> .env
          cat .env

      - name: Export Environment Variables
        run: |
          export $(cat .env | xargs)
        shell: bash
        # env:
        #   REACT_APP_OKTA_CLIENT_ID: ${{ secrets.REACT_APP_OKTA_CLIENT_ID }}


  build_and_deploy_job:
    if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.action != 'closed')
    runs-on: ubuntu-latest
    name: Build and Deploy Job


    steps:
      - uses: actions/checkout@v3
        with:
          submodules: true
          lfs: false
      - name: Build And Deploy
        id: builddeploy
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_DELIGHTFUL_PLANT_0C3705F10 }}
          repo_token: ${{ secrets.GITHUB_TOKEN }} # Used for Github integrations (i.e. PR comments)
          action: "upload"
          ###### Repository/Build Configurations - These values can be configured to match your app requirements. ######
          # For more information regarding Static Web App workflow configurations, please visit: https://aka.ms/swaworkflowconfig
          app_location: "/" # App source code path
          api_location: "" # Api source code path - optional
          output_location: "build" # Built app content directory - optional
          ###### End of Repository/Build Configurations ######

  close_pull_request_job:
    if: github.event_name == 'pull_request' && github.event.action == 'closed'
    runs-on: ubuntu-latest
    name: Close Pull Request Job
    steps:
      - name: Close Pull Request
        id: closepullrequest
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_DELIGHTFUL_PLANT_0C3705F10 }}
          action: "close"
